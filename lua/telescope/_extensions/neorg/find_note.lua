local utils = require("neorg.utils")
local scandir = require("plenary.scandir")
return function()
    P(#require("neorg.core"))
    -- local wksp = utils.get_current_workspace()
    -- local files = utils.get_files(wksp[1])
end
